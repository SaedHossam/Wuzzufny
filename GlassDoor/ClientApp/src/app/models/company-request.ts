export class CompanyRequest {


  constructor(public id: number, public name: string, public industry: string, public size: string, public firstName: string, public lastName: string,
    public title: string, public phoneNumber: string, public email: string) {
  }
}
